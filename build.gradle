allprojects {
    version("3.2.0-CN")
    plugins.apply(VersionIdPlugin.class)
}

gradle.buildFinished {
    project.buildDir.deleteDir()
}

class VersionIdPlugin implements Plugin<Project> {
    @Override
    void apply(Project project) {
        project.ext.versionId = 12.toString()
    }
}