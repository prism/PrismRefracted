pluginManagement {
    repositories {
        gradlePluginPortal()

        maven { url = "https://repo.jpenilla.xyz/snapshots/" }
    }
}

rootProject.name = 'PrismRefracted'
include 'Prism-Api', 'Prism'

