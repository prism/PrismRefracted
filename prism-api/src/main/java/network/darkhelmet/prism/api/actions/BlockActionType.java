package network.darkhelmet.prism.api.actions;

public class BlockActionType extends ActionType {
    /**
     * Construct a new block action type.
     *
     * @param key The key
     * @param resultType The result type
     */
    public BlockActionType(String key, ActionResultType resultType) {
        super(key, resultType);
    }
}
